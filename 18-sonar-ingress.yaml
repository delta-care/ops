apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: sonarqube
  namespace: ops
spec:
  rules:
  - host: sonar.deltacare.xyz
    http:
      paths:
      - path: /
        backend:
          serviceName: sonarqube
          servicePort: 80
